<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>UNTAMED STREETWEAR</title>

<style>
:root{
  --bg:#0b0b0b;
  --panel:#111;
  --text:#f5f5f5;
  --muted:#9a9a9a;
  --accent:#e63946;
}

*{
  margin:0;
  padding:0;
  box-sizing:border-box;
}

body{
  font-family: Arial, Helvetica, sans-serif;
  background:var(--bg);
  color:var(--text);
  line-height:1.5;
  overflow-x:hidden;
}

a{
  text-decoration:none;
  color:inherit;
}

.container{
  max-width:1200px;
  margin:auto;
  padding:0 24px;
}

/* ---------- HERO ---------- */

.hero{
  min-height:100vh;
  display:flex;
  align-items:center;
  position:relative;
  background:
    radial-gradient(circle at 20% 20%, #1b1b1b, transparent 60%),
    radial-gradient(circle at 80% 80%, #1b1b1b, transparent 60%);
}

.hero::after{
  content:"UNTAMED";
  position:absolute;
  bottom:10%;
  right:-10%;
  font-size:18rem;
  font-weight:900;
  color:#111;
  letter-spacing:-0.05em;
  pointer-events:none;
}

.hero-content{
  max-width:700px;
}

.hero h1{
  font-size:clamp(3rem,8vw,6.5rem);
  font-weight:900;
  text-transform:uppercase;
}

.hero h1 span{
  color:var(--accent);
}

.hero p{
  margin:24px 0 40px;
  font-size:1.4rem;
  color:var(--muted);
}

.btn{
  display:inline-block;
  padding:18px 40px;
  border:2px solid var(--text);
  text-transform:uppercase;
  font-weight:800;
  letter-spacing:0.12em;
  transition:0.3s;
}

.btn:hover{
  background:var(--text);
  color:#000;
}

/* ---------- SECTION BASE ---------- */

section{
  padding:100px 0;
  position:relative;
}

.section-label{
  color:var(--accent);
  letter-spacing:0.3em;
  font-weight:700;
  margin-bottom:16px;
}

.section-title{
  font-size:clamp(2.5rem,6vw,4rem);
  font-weight:900;
  text-transform:uppercase;
  margin-bottom:40px;
}

/* ---------- MANIFESTO ---------- */

.manifesto{
  background:var(--panel);
}

.manifesto p{
  font-size:1.3rem;
  max-width:900px;
}

.manifesto p strong{
  color:var(--accent);
}

/* ---------- VISUAL DROP ---------- */

.drop{
  display:grid;
  grid-template-columns:1fr 1fr;
  gap:60px;
  align-items:center;
}

.drop-text h3{
  font-size:2.5rem;
  font-weight:900;
  text-transform:uppercase;
}

.drop-text p{
  margin:20px 0;
  color:var(--muted);
  font-size:1.2rem;
}

.drop-image{
  position:relative;
}

.drop-image img{
  width:100%;
  height:460px;
  object-fit:cover;
  filter:grayscale(20%);
  border:1px solid #222;
}

.drop-image::after{
  content:"01";
  position:absolute;
  top:-40px;
  left:-40px;
  font-size:6rem;
  font-weight:900;
  color:var(--accent);
}

/* ---------- PRODUCT GRID ---------- */

.products{
  background:var(--panel);
}

.grid{
  display:grid;
  grid-template-columns:repeat(auto-fit,minmax(260px,1fr));
  gap:40px;
}

.card{
  border:1px solid #222;
  padding:30px;
  transition:0.3s;
  position:relative;
}

.card::before{
  content:"→";
  position:absolute;
  top:20px;
  right:20px;
  color:var(--accent);
  font-size:1.5rem;
}

.card:hover{
  transform:translateY(-10px);
  border-color:var(--accent);
}

.card h4{
  font-size:1.6rem;
  font-weight:800;
  margin-bottom:10px;
  text-transform:uppercase;
}

.card p{
  color:var(--muted);
}

/* ---------- ANIME POSTERS ---------- */

.posters{
  text-align:center;
}

.poster-grid{
  margin-top:60px;
  display:grid;
  grid-template-columns:repeat(auto-fit,minmax(280px,1fr));
  gap:40px;
}

.poster{
  background:#0f0f0f;
  border:1px solid #222;
}

.poster img{
  width:100%;
  height:420px;
  object-fit:cover;
}

.poster div{
  padding:20px;
}

.poster h5{
  font-size:1.4rem;
  text-transform:uppercase;
}

.poster span{
  color:var(--muted);
  font-style:italic;
}

/* ---------- COMMUNITY ---------- */

.community p{
  max-width:800px;
  font-size:1.3rem;
  color:var(--muted);
}

/* ---------- FINAL CTA ---------- */

.cta{
  background:linear-gradient(135deg,#000,#1a1a1a);
  text-align:center;
}

.cta h2{
  font-size:clamp(3rem,7vw,5rem);
  font-weight:900;
  text-transform:uppercase;
  margin-bottom:40px;
}

@media(max-width:900px){
  .drop{
    grid-template-columns:1fr;
  }
  .hero::after{
    display:none;
  }
}
</style>
</head>

<body>

<!-- HERO -->
<section class="hero">
  <div class="container hero-content">
    <h1>UNTAMED<br><span>STREETWEAR</span></h1>
    <p>Built for those who refuse to move quietly.</p>
    <a href="#drop" class="btn">Enter the Wild</a>
  </div>
</section>

<!-- MANIFESTO -->
<section class="manifesto">
  <div class="container">
    <div class="section-label">// MANIFESTO</div>
    <h2 class="section-title">Break What Tries to Shape You</h2>
    <p>
      UNTAMED exists for people who don’t blend in, don’t slow down,
      and don’t ask permission. <strong>This is street culture without the costume.</strong>
    </p>
  </div>
</section>

<!-- DROP -->
<section id="drop">
  <div class="container drop">
    <div class="drop-text">
      <div class="section-label">// DROP 01</div>
      <h3>Midnight Riot</h3>
      <p>Oversized silhouettes. Silent aggression. Built for after dark.</p>
      <a href="#" class="btn">Explore Drop</a>
    </div>
    <div class="drop-image">
      <img src="https://images.unsplash.com/photo-1520975916090-3105956dac38">
    </div>
  </div>
</section>

<!-- PRODUCTS -->
<section class="products">
  <div class="container">
    <div class="section-label">// ESSENTIALS</div>
    <h2 class="section-title">Built Different</h2>
    <div class="grid">
      <div class="card">
        <h4>Statement Hoodies</h4>
        <p>Heavy presence. Zero compromise.</p>
      </div>
      <div class="card">
        <h4>Cargo Pants</h4>
        <p>Function meets rebellion.</p>
      </div>
      <div class="card">
        <h4>Signature Tees</h4>
        <p>Say nothing. Mean everything.</p>
      </div>
    </div>
  </div>
</section>

<!-- ANIME POSTERS -->
<section class="posters">
  <div class="container">
    <div class="section-label">// VISUAL STORY</div>
    <h2 class="section-title">Anime-Inspired Frames</h2>
    <div class="poster-grid">
      <div class="poster">
        <img src="https://images.unsplash.com/photo-1503342217505-b0a15ec3261c">
        <div>
          <h5>Untamed Shirt</h5>
          <span>Soft chaos</span>
        </div>
      </div>
      <div class="poster">
        <img src="https://images.unsplash.com/photo-1512436991641-6745cdb1723f">
        <div>
          <h5>Untamed Pants</h5>
          <span>Freedom in motion</span>
        </div>
      </div>
    </div>
  </div>
</section>

<!-- COMMUNITY -->
<section class="community">
  <div class="container">
    <div class="section-label">// COMMUNITY</div>
    <h2 class="section-title">Not Everyone Belongs Here</h2>
    <p>
      Artists. Skaters. Night thinkers. People who build their own lane.
      If you blend in — this isn’t for you.
    </p>
  </div>
</section>

<!-- CTA -->
<section class="cta">
  <div class="container">
    <h2>Stop Following<br>Start Leading</h2>
    <a href="#" class="btn">Claim Your Piece</a>
  </div>
</section>

</body>
</html>
